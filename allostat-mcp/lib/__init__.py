"""Allostat-MCP wrapper-plugin library.

Modules:
  mcp_client       — Streamable-HTTP MCP client (stdlib-only)
  local_state      — read/write helpers for the operator's local
                     .allostat/ directory (observations.jsonl, silos,
                     state.json, drift_seen)
  excerpt          — context-excerpt composer (η.2)
  classifier       — routing classifier (η.2)
"""
