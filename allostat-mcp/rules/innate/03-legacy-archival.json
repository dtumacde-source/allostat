{
  "id": "innate-03",
  "name": "LEGACY archival on every rollout",
  "severity": "lethal",
  "tier": "innate",
  "description": "When a rollout / deploy / new version of an asset ships (web zip uploaded, PDF re-rendered, slide deck re-exported, package re-uploaded), every superseded file in the prior source folder gets renamed with the suffix _LEGACY_pre_YYYYMMDD_rollout (rollout date, not today's date). Files with this suffix are read-only history. Never edited. Never used as deploy source.\n",
  "trigger": {
    "match_any": [
      {
        "context": "post_deploy_detected",
        "signal": "deploy artifact ships in folder X"
      },
      {
        "context": "post_upload_detected",
        "signal": "wrangler publish / vercel deploy / netlify deploy / git push to deploy branch"
      },
      {
        "context": "post_export_detected",
        "signal": "PDF re-rendered, deck re-exported, .docx re-saved as deliverable"
      },
      {
        "operation": [
          "write",
          "edit"
        ],
        "file_pattern": "**/*_LEGACY_*",
        "action": "refuse_all"
      }
    ]
  },
  "response": {
    "action": "prompt_archival",
    "display": "red_box",
    "message": "📦 ROLLOUT ARCHIVAL REQUIRED\n\nDetected: {rollout_event}\nFolder with superseded files: {folder}\n\nTo preserve rollback safety, every prior file in that folder needs the\nsuffix _LEGACY_pre_{rollout_date}_rollout. I'll do this now unless you\nredirect.\n\nFiles to archive: {file_list}\nRename pattern: foo.html → foo_LEGACY_pre_{rollout_date}_rollout.html\n\nProceed? Type \"yes\" or specify exclusions.\n"
  },
  "edit_protection": {
    "pattern": "**/*_LEGACY_*",
    "action": "hard_pause",
    "message": "⛔ ATTEMPTED EDIT ON LEGACY FILE\n\nFile: {file_path}\n\nFiles marked _LEGACY_pre_*_rollout are read-only history. They exist\nso you can roll back if a deploy goes wrong. Editing them defeats\nthat purpose.\n\nIf you genuinely need to edit, copy to a new filename first.\n"
  },
  "why_lethal": "Editing the wrong copy is one of the top causes of lost work in long multi-folder projects. The LEGACY archival convention is the single most-effective prevention across long-lived projects with multiple deploy cycles.\n"
}
